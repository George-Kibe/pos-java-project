/** The shop's name, on the header, the tab title and every receipt. */
export const BRAND_NAME = "Realhive Group of Supermarkets POS";

/** The business itself, where the product name would be wrong (a receipt's heading). */
export const BUSINESS_NAME = "Realhive Group of Supermarkets";

/**
 * The mark, drawn from the supplied logo by scripts/brand/generate.py. Every brand image - this,
 * the favicons, the app icons, the receipt bitmap, the email and PDF logos - comes from there.
 */
export const BRAND_LOGO = "/brand/realhive-logo.svg";
